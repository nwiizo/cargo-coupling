# E2E Test

Use [the e2e-test skill](../skills/e2e-test/SKILL.md) for this command.
Interpret `$ARGUMENTS` using that skill's options and follow its fixture, coverage,
and reporting guidance. Keep scenario details in the skill's linked reference.
