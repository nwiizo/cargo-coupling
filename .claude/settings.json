{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git commit:*)",
        "hooks": [
          {
            "type": "command",
            "command": "cargo fmt --all --check && cargo clippy --all-targets --all-features -- -D warnings"
          }
        ]
      }
    ]
  }
}
